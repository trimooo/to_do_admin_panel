import React from "react";
declare type ModalProps = {
    visible: boolean;
    onClose: () => void;
    className?: string;
    children: React.ReactNode;
    header?: React.ReactNode;
    footer?: React.ReactNode;
    overlay?: boolean;
};
export declare const Modal: ({ visible, onClose, className, children, header, footer, overlay, }: ModalProps) => JSX.Element;
export {};
//# sourceMappingURL=modal.d.ts.map