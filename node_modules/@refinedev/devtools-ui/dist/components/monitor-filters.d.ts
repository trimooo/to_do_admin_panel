import { Activity } from "src/interfaces/activity";
export declare type Filters = {
    scope: string[];
    hook: string[];
    parent: string[];
    resource: string | undefined;
    status: Array<"loading" | "success" | "error" | "idle" | "paused">;
};
declare type Props = {
    filters: Filters;
    activities: Activity[];
    onSubmit: (filters: Filters) => void;
};
export declare const MonitorFilters: ({ filters: defaultFilters, activities, onSubmit, }: Props) => JSX.Element;
export {};
//# sourceMappingURL=monitor-filters.d.ts.map